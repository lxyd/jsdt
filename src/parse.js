define(['./model'], function(model) {
    // FUTURE : delegate to ./parse-versions/parse-X by requireing
    //          particular parse module depending on obj.version

    function parse(obj) {
        // TODO : implement
    }
    
    return parse;
});